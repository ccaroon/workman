<div class="header">
    <b>Goal</b>
</div>

<div style="font-size:large;font-weight:bold">
    #<%= display_goal @goal, :priority %>) <%= raw display_goal @goal, :name %> - <%= display_goal @goal, :percent_complete %>% complete.
</div>

<div style="padding:15px">
    <%= raw display_goal @goal, :description %>
</div>

<div class="error_message">
    <%= raw display_goal @goal, :obstacles %>
</div>

<p>

<%= link_to 'Edit', edit_goal_path(@goal) %>
